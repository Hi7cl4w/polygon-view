<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorPrimaryDark"
    tools:context=".MainActivity">


    <com.pinocks.polygonview.PolygonView
        android:id="@+id/polygonview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:buttonBottomLeftDrawable="@android:drawable/ic_input_get"
        app:buttonBottomRightDrawable="@android:drawable/ic_dialog_email"
        app:buttonCenterDrawable="@android:drawable/ic_menu_add"
        app:buttonTopLeftDrawable="@android:drawable/ic_dialog_info"
        app:buttonTopRightDrawable="@android:drawable/ic_dialog_alert"
        />

</android.support.constraint.ConstraintLayout>